# ─────────────────────────────────────────────────────────────────────────────
#  Voice AI Agent — Python Dependencies
#
#  Only packages with NO pipecat overlap are pinned.
#  Everything else is unpinned — pip resolves it via pipecat's constraints.
# ─────────────────────────────────────────────────────────────────────────────

# Core AI pipeline
# Owns: livekit, livekit-api, openai, deepgram, pydantic, tenacity, httpx
pipecat-ai[deepgram,openai,elevenlabs,silero,livekit]==0.0.47

# Web server
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Twilio SDK
twilio==9.2.3

# Knowledge base
chromadb==0.5.3
sentence-transformers==3.0.1

# Metrics
prometheus-client==0.20.0
prometheus-fastapi-instrumentator==7.0.0

# Resilience
circuitbreaker==2.0.0

# Utilities
python-dotenv==1.0.1
pydantic-settings>=2.3.4
structlog==24.2.0
asyncio-pool==0.6.0
numpy==1.26.4
aiofiles==23.2.1