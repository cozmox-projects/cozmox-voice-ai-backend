# ─────────────────────────────────────────────────────────────────────────────
#  Voice AI Agent — Python Dependencies
#
#  Strategy: let pipecat-ai resolve its own sub-dependencies (livekit,
#  pydantic, openai, deepgram etc). Only pin packages pipecat doesn't touch.
# ─────────────────────────────────────────────────────────────────────────────

# Core AI pipeline — owns livekit, pydantic, openai, deepgram as sub-deps
pipecat-ai[deepgram,openai,elevenlabs,silero,livekit]==0.0.47

# Web server
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Twilio SDK
twilio==9.2.3

# LiveKit API (admin SDK — separate from livekit client owned by pipecat)
livekit-api==0.6.0

# Knowledge base
chromadb==0.5.3
sentence-transformers==3.0.1

# Metrics
prometheus-client==0.20.0
prometheus-fastapi-instrumentator==7.0.0

# Resilience
tenacity==8.4.1
circuitbreaker==2.0.0

# Utilities
python-dotenv==1.0.1
pydantic-settings>=2.3.4
structlog==24.2.0
httpx==0.27.0
asyncio-pool==0.6.0
numpy==1.26.4
aiofiles==23.2.1